import styled from 'styled-components/native';

export const TopBarHeader = styled.SafeAreaView`
  background: rgba(0, 0, 0, 0.3);
  justify-content: center;
  align-items: center;
`;

export const Logo = styled.Image`
  margin: 15px 0;
  width: 24px;
  height: 23px;
`;
